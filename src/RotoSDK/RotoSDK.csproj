<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
	  <ImplicitUsings>enable</ImplicitUsings>
	  <Nullable>disable</Nullable>
	  <Configurations>Debug;Release;Dev</Configurations>
    <Platforms>x64</Platforms>    
  </PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.2" />
		<PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.3" />		
		<PackageReference Include="System.Management" Version="9.0.6" />
	
		<!--<PackageReference Include="Sharpie.Engine" Version="1.0.8" />-->
		<PackageReference Include="Sharpie.Helpers.Core" Version="1.0.8" />
		<PackageReference Include="Sharpie.Helpers.Telemetry" Version="1.0.8" />
		<!--<PackageReference Include="Sharpie.Plugins.Speech" Version="1.0.8" />
		<PackageReference Include="Sharpie.Plugins.SharpDX" Version="1.0.8" />
		<PackageReference Include="Sharpie.Plugins.UsbWatcher" Version="1.0.8" />-->
	</ItemGroup>
  

  <ItemGroup>
    <PackageReference Include="System.Management" Version="9.0.9" />
  </ItemGroup>

  <!-- Keep the files as content (None), but custom copy logic will handle placing them in TargetDir -->
  <ItemGroup>
    <None Update="Libs\RotoSdk\HIDApi.dll">
      <CopyToOutputDirectory>Never</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <!-- Copy architecture-specific HIDApi.dll into $(TargetDir) -->
  <!--<Target Name="CopyHIDApi" AfterTargets="Build">
    --><!-- Normalize PlatformTarget (AnyCPU at build time can't decide, so skip unless explicitly x64/x86) --><!--
    --><!--<PropertyGroup>
      <_ResolvedPlatform Condition="'$(PlatformTarget)' == ''">$(Platform)</_ResolvedPlatform>
      <_ResolvedPlatform Condition="'$(_ResolvedPlatform)' == ''">AnyCPU</_ResolvedPlatform>
    </PropertyGroup>--><!--

    <ItemGroup>
      <_HidSource Include="$(ProjectDir)Libs\RotoSdk\HIDApi.dll" Condition="Exists('$(ProjectDir)Libs\RotoSdk\HIDApi.dll') " />
    </ItemGroup>

    <Message Text="Copying HIDApi.dll to $(TargetDir)Libs\RotoSdk" Importance="High" Condition="'@(_HidSource)' != ''" />
    <Copy SourceFiles="@(_HidSource)" DestinationFolder="$(TargetDir)Libs\RotoSdk" SkipUnchangedFiles="true" Condition="'@(_HidSource)' != ''" />
    <Warning Text="No platform-specific $(_HidSource) copied (PlatformTarget=$(PlatformTarget))." Condition="'@(_HidSource)' == ''" />
  </Target>-->
	
	
</Project>
